# V4 documentation transition checkpoint

Historical scope below was documentation-only. The subsequent authorized correction/P0 execution is recorded at the end; no production effects or Git history rewriting are permitted.

Baseline: `8e50d68efbf2bec2aabfb2fe3226e01635de2adb`, inspected 2026-09-22.

- [x] Inventory before consolidation: `inventory.json` contains 360 files, including all current docs, master README, prompts, ArchV4 sources, repository-owned skill instructions, and discovered worktree/checkpoint/visual evidence. Dependency installations and generated caches are explicitly excluded.
- [x] Read both complete ArchV4 sources and inspect present schema, workflow registration, control/auth boundaries, adapters, tests, and Compose configuration.
- [x] Draft a standalone blueprint with 70 requirement-to-phase/dependency/test/migration/release mappings and 17 current-state evidence groups.
- [x] Validate before consolidation: `python3 docs/v4/check.py --draft --self-test` passed all mappings, source incorporation, 249 evidence locks, 360 original inventory hashes and six negative controls.
- [x] Archive 62 superseded prose documents with exact-byte recovery, retain instruction files and historical visual artifacts, repair navigation and preserve heading bookmarks.
- [x] Re-run documentation checks and six negative controls; validate 82 Markdown files, five local HTML visuals, all 360 dispositions and 249 unchanged application evidence files. A simulated clean checkout also passes without ignored local/worktree artifacts.
- [x] Existing Phase 7–8, Phase 9 and browser documentation/command-contract checks pass. Application diff is empty; Git HEAD remains the original baseline. No application feature or Git history change occurred.

Documentation objective complete. Future implementation begins at blueprint P0; all V4 application phases remain planned. Fresh application evidence is 226 passes and six browser failures due to missing externally provisioned Chromium, recorded in `validation.md` and blueprint C20. Documentation CI is configured, but remote workflow execution and branch protection are not claimed.

Findings carried into the blueprint: current auth accepts a caller scope header after one shared bearer token; durable job creation and Temporal starts are separate calls without a transactional outbox; no V4 cycle/context/observation/experiment/training entities were found in the schema/module inventory; current YouTube adapter starts an upload session but does not upload or verify publication; Garage configuration referenced by Compose is absent. These remain explicit future release obligations.

## Correction and P0 execution — 2026-09-22

Continue the existing dirty main workspace; preserve its documentation consolidation and archive. Do not merge stale worktrees or rewrite history. User authorizes P0 application changes, not P1–P7 implementation.

- [x] Reinspect configuration, authentication dependency, canonical schema, storage adapters, telemetry, worker and test setup; reuse existing PostgreSQL/Temporal, secret/scoping contracts and OTel SDK.
- [x] Red: `python3 docs/v4/test_checks.py` failed twice (missing gate validator; P0 prematurely requiring outbox).
- [x] Correct prose gates and split five foundational obligations without deleting original requirements. Resolve re-admission, G0/G1 accounting, optional capabilities/R58 and promotion/invalidation sequencing in the blueprint.
- [x] Green: gate tests 2/2; `check.py --self-test` passes 75 mapped obligations and unchanged original sources/evidence.
- [x] Validate Markdown structure/literals and render six Mermaid diagrams before application changes.
- [x] Red/green P0 identity and isolated effects-locked configuration using maintained JWT verification and database authority/audit.
- [x] Red/green immutable storage/readback/integrity and retention-safe lifecycle hooks; qualify disposable real S3 separately from mocks.
- [x] Red/green trace propagation/redaction and dependency readiness; full outbox test remains P1.
- [x] Run regression, migrations/rollback, review actual evidence, refresh traceability lock and explicitly hold RG0 for outstanding external qualification.

Dependency decision: adopt already-installed PyJWT 2.14.0 (MIT) with cryptography 50.0.1 for fixed-RS256 issuer/audience/required-time verification, not a custom token issuer or OAuth server. Server-side grants remain canonical. Official API review: https://pyjwt.readthedocs.io/en/stable/api.html . Retain boto3/S3 and OTel SDK; conditional writes require actual service verification, not an assumed Garage capability. References: https://docs.aws.amazon.com/AmazonS3/latest/userguide/conditional-writes.html and https://garagehq.deuxfleurs.fr/documentation/reference-manual/s3-compatibility/ . Mermaid 12.0.0 (MIT, npm integrity verified) is an isolated documentation verifier, not an application dependency.

### 2026-09-23 verification checkpoint

All six authoritative diagrams parse/render using Mermaid 12.0.0 and existing `/usr/bin/chromium`; structural checks of 82 active Markdown documents find no export corruption. Original sources remain byte-identical. Artifacts: `artifacts/v4-p0/diagrams/` (local, regenerable with `verify_diagrams.py`). Gate and Markdown negative tests pass 6/6.

P0 changes in progress: isolated JWT/database-authority API; append-only access decisions; effects and worker lockout; bounded local control; immutable object adapters, persisted verification/quarantine/reference/retention hooks; exact active OTel span identities and redaction; additive migrations 0013/0014. Migration was initially rejected for multiple prepared SQL commands, repaired with per-statement execution. A full nonbrowser regression found a memory-store metadata compatibility break (249 pass, 1 fail); repaired by keeping the integrity digest private, preserving caller metadata. Focused repair/migration/auth/trace tests then passed 31/31. Final full regression is running, not yet claimed green.

Real pinned Garage v2.3.0 **ignored `If-None-Match: *`**, overwriting an existing key. Added a startup/first-write capability probe that rejects such an endpoint before storing caller bytes. Two positive qualification tests were correctly NOT RUN; the negative real-service rejection test passed. Do not call this a qualified storage service. Investigating mature replacement rather than stopping at a locally resolvable dependency: SeaweedFS 4.47 (Apache-2.0, released 2026-09-14, tag `c5073360007d28385a33426a42ac3e4ec504c5a3`) documents atomic conditional writes; testing an isolated digest-pinned container next. Sources: [conditional operations](https://github.com/seaweedfs/seaweedfs/wiki/S3-Conditional-Operations), [release](https://github.com/seaweedfs/seaweedfs/releases/tag/4.47), [license](https://github.com/seaweedfs/seaweedfs/blob/4.47/LICENSE). MinIO was not selected because its upstream repository is archived; no custom storage engine is being built.

Provisioned the existing pinned Playwright headless runtime (Chromium 151.0.7922.34, revision 1234) using `.venv/bin/python -m playwright install chromium --only-shell`; this is local test tooling, not production browser enablement. Independent P0 code review is running per requesting-code-review skill. No commit, push, history rewrite, live provider operation, or P1 implementation occurred.

### Dependency recovery and inspection

SeaweedFS 4.47 image digest `sha256:ce9e796f1fe6f06968f4c04bdaf8f678dad9c8acdfef3d244133d71bfa6bf882` passed the real storage suite, including concurrent create, exact retry, corruption/missing bytes, scoped credentials and lifecycle quarantine. Positive tests now use this qualified disposable backend and no longer skip; Garage remains an explicit negative regression. No production backend was replaced or exposed. The original full non-live regression, collected before this replacement, completed with 259 passed/2 skipped; it is superseded by a new full run on final code.

Independent review could not run: the reviewer tool returned an account usage-limit error, confirmed by the agent status, not a review approval. No repeated retry can supply independent evidence without the external quota changing. Root performed local inspection instead: fixed an invented intermediate API parent span; prevented automatic exception-message export; enforced inactive-workspace denial; bounded S3 reads/writes; and fixed the delete-before-database-commit crash race by rechecking stored bytes under the reference lock and persisting quarantine before rejecting a missing object. Added targeted regressions; final full verification remains in flight. Independent review is explicitly NOT RUN and remains required before production rollout.

Read-only local inventory at migration `0014_object_inventory`: 87 tables, 565 nonterminal local job/history references, 940 fixture external-effect references, 100 reserved fixture liabilities at capture time; these are accumulated local test records, not production obligations and are not automatically resumed/settled. Safe inventory is `artifacts/v4-p0/runtime-inventory.json`, reproducible with `capture_runtime.py`. Production history/reconciliation inventory still requires its actual authorized environment.

### Final executable checkpoint — 2026-09-23

- Corrected blueprint, future phase decisions, original source coverage and all six diagrams validated; 75 mapped obligations remain acyclic. Documentation regression tests: 7 passed; full documentation validator and six original negative controls pass, including a simulated clean checkout.
- R22 edge-secret baseline now pins server-owned destination/audience/workspace/provider/version/exact scopes; leases expire at reveal, revoke and cannot serialize or bypass through the legacy resolver. Red import failure followed by passing focused tests is preserved.
- Final P0-focused suite: **37 passed**. Final complete non-live suite: **269 passed, zero failed, zero skipped**, 254.27 seconds; browser and durable recovery tests included. A prior 266-pass/1-failure run exposed real storage startup readiness; repaired with verified data-plane warmup rather than accepting bucket creation alone.
- Refreshed 262 code/config/test evidence locks after local inspection; original 249-file baseline lock remains in `evidence-baseline.json`. All original source/archive/instruction hashes still verify. Compose validation, dependency consistency, original documentation shell tests and whitespace checks pass.
- Local implementation/verification is complete. **RG0 production release remains HELD**, with NOT RUN external issuer/TLS/key-rotation/restricted database-role, encrypted managed-storage/restore, telemetry/alerts, remote repository enforcement and independent review requirements in `p0-release.json`. The reviewer service is blocked by external quota. Required production profile environment inputs are absent; fixture values are not approvals.
- No production effects, production ingress, P1 code, commit, push or history rewrite. Resume at the explicit RG0 external qualification items, not at reimplementing the completed P0 work. Do not enter P1 or represent RG0 as passed until those applicable checks are resolved and evidenced.

### Requested branch publication — 2026-09-23

User subsequently requested a commit and push on a new branch. Publication target: `origin/feat/archv4-p0-foundation`, based on `8e50d68efbf2bec2aabfb2fe3226e01635de2adb`. Include the preserved documentation consolidation and P0 work together; leave `main` and the other worktrees unchanged. This does not release RG0 or authorize production effects. Pre-commit verification is being rerun on this tree; fresh results are retained separately under `artifacts/v4-p0/branch-verification.*` so the original qualification evidence hashes remain valid.

The interrupted pre-commit run produced no final result and is not counted as passing. Its replacement completed successfully: **269 passed, zero failed, zero skipped**, 206.71 seconds, with one existing Starlette/AnyIO deprecation warning. Fresh documentation regression tests: **7 passed**; the complete documentation validator and six negative controls pass (75 obligations, 262 evidence files, 360 inventory entries, 82 Markdown files and five HTML files). Dependency consistency and staged whitespace checks pass. RG0 remains **HELD**; this publication is a source checkpoint, not production authorization.

### Review corrections and P1 development entry — 2026-09-23

Baseline inspected: `ca37f6f`, clean feature branch. Verified unresolved defects: §6 conflates post-admission deferral with recovery despite R09; `release_errors` accepts empty external checks and ignores local qualification; the identity query takes `FOR SHARE`, which needs UPDATE privilege not granted by the documented read-only authority policy. Remote inspection finds successful documentation run 35811429145 on the baseline, but main is unprotected and has no applicable rulesets. This is a failed enforcement check, not merely NOT RUN.

Execution plan (inline, using the existing feature branch; no production enablement or merge):

1. Red/green release-validator tests: require the exact external-check multiset, recognized statuses, nonempty PASS evidence, successful evidenced local qualification and a separately validated development-entry checklist. Preserve all production obligations.
2. Add a narrowly scoped security-definer read/lock function in additive migration 0015, revoke PUBLIC execute, and exercise authorization with a genuine non-owner login having no authority DML/DDL rights. Verify revocation and transaction ordering. Do not remove the lock or grant authority UPDATE.
3. Correct normative deferral semantics and development-versus-production gates in this blueprint; original ArchV4 source snapshots remain immutable. Configure/check required remote CI before any implementation merge, preserve raw nonsecret inspection evidence, and record failure or missing privileges honestly.
4. Run focused P0/documentation checks and evidence the local development prerequisites. Only after that local gate passes, add the first P1 increment: typed local GoalSpec/intent/admission/context and bounded deferral/recovery identity contracts, PostgreSQL constraints and concurrency acceptance tests. All external dispatch remains disabled. Later P1 increments retain their own explicit acceptance gates; do not label the entire phase complete from an admission slice.
5. Refresh reviewed source hashes, requirement acceptance mappings, release ledger and this checkpoint; run focused and full non-live regressions, documentation validation and six-diagram validation. Capture current-commit CI and branch enforcement separately from local test evidence. No history rewrite or main merge.

Correction evidence: release red stage reproduced five assertion failures plus missing-field KeyError; corrected validator passes twelve documentation tests. Restricted-role red stage reproduced the HTTP 503 permission failure and absent delegated function; migration 0015 plus the narrow EXECUTE grant gives sixteen passing identity/migration tests. A migration attempt exposed asyncpg's one-prepared-command restriction; splitting function creation and PUBLIC revocation into separate statements in the same migration transaction fixed it. Full P0 focus is 39 PASS. Main protection is now configured and read back: strict `verify` and `p0-regression` checks pinned to GitHub Actions app 15368, administrators enforced, current independent PR approval required, force pushes/deletion disabled. Baseline commit has only the old documentation check; the new regression check remains missing until this workflow is published. Merge stays HELD.

P1 increment design: start with a dry-run-only internal admission/lifecycle service and additive canonical sidecars, not a premature production coordinator. Use existing PostgreSQL, identity authority and trace contracts; no new infrastructure or dependency. Model three separate state transitions explicitly, persist admission revisions and frozen context, serialize goal/intent transitions, append audit/closure events and reject payload changes under an existing logical slot. Scope-check current subject/grants on every command. Duplicate pre-admission wakes are compare-and-set; recovery never creates a cycle; a strategic closure can only lead to an authorized next-slot intent coalescing with the schedule. Nonzero spend/live execution is rejected in this first slice; G0 paid commitments, G1 transfer, outbox/Temporal, permit/review commands and API/CLI/SDK integration remain later P1 increments. This is deliberate incremental delivery, not an RG1 completion claim.

DG1 passed before P1 code: 39 focused P0, 271 full non-live, twelve documentation tests, six Mermaid renders and the full 75-obligation validator. Added durable `development-entry-evidence.json`; raw local evidence is not the only persisted proof. This expressly supersedes the earlier checkpoint instruction prohibiting local P1 before production RG0.

P1a red/green: first collection failed because no cycles module existed. Thirteen admission/lifecycle acceptance cases now cover the three deferral paths, concurrent review/admission and successor/schedule coalescing, current scope/revocation, expired reviews, bounded recovery, immutable context/business identity, and simulated crash before commit with fresh-service recovery. Direct SQL reopening/identity rewriting initially succeeded incorrectly; additive migration 0017 now rejects it. Combined admission/identity/migration suite: 29 PASS. An additional migration regression proves populated V4 rollback is refused without deleting its history. Full regression and current-head CI are being refreshed; do not infer their final status from these focused results.

Independent reviewer found two Important validator issues: suite errors/exit status were ignored and host-local paths could masquerade as durable evidence. Both reproduced red and were repaired; documentation tests now include explicit command evidence parsing and repository-bound durable reports. Re-review requested. No production qualification was invented, no main merge occurred, and no legacy fixture dispatch was enabled by P1a.

Local P1a checkpoint: **284 full non-live tests PASS**, zero failures/errors/skips, 257.86 seconds; thirteen documentation tests and complete source/archive/link/DAG validation PASS with 269 reviewed application evidence files. Six unchanged source diagrams rendered successfully. Fresh independent re-review confirms both validator findings resolved and no unresolved Critical/Important issue in this slice. `p1-increment-evidence.json` persists execution evidence. A separate migration rerun passes after adding populated-V4 rollback preservation assertions. RG1 is explicitly HELD for the listed remaining increments, not treated as complete. Publishing the source checkpoint on the existing feature branch is solely to execute current-head CI; main remains protected and unmerged.

Remote run 35860805982 on `9445348`: documentation PASS; application **280 PASS, 4 ERROR** (SeaweedFS fixture setup). Investigation inspected the pinned image entrypoint: it drops to UID 1000, while GitHub's config owner differs and the credentials file correctly has mode 0600. A controlled UID-1001 file-read probe reproduced denial under UID 1000 and success under the matching UID. Repair runs the container as the host fixture UID/GID and makes the ephemeral data mount owned by that same identity; credentials remain 0600, root filesystem read-only and resource bounds unchanged. Five real-storage tests PASS locally. Added explicit container UID/config readability assertions. No retry-count increase, permission broadening or test skip. Also hardened suite result fields so missing failed/errors/skipped/exit_code are rejected rather than defaulted to success. Repaired-head CI must pass before any merge.

Remote repair verified: run 35867175746 on `b1969c821485952a96433fb0ac7e4ea3fed20e31` passes both required checks. `repository-enforcement-evidence.json` records actual check IDs, successful conclusions and read-back protection; RG0 repository obligation now PASS for that inspected commit. The other four production checks remain NOT RUN, RG0 HELD, main unmerged. Future implementation heads require fresh CI/protection inspection. Pending/missing CI now reports NOT RUN rather than FAIL; neither permits merge.

Ruling: implement the local transactional outbox increment next, before paid G0 accounting — admission is still strictly zero-spend and cannot dispatch a real provider, so exercising durable identity delivery does not bypass G0/G1. Paid admission remains disabled until its separate ledger work passes. Sequence: red atomic enqueue/ordered lease/retry/dead-letter/inbox tests; additive outbox/inbox migration; connect existing admission/recovery/closure transactions to immutable messages; add a fixed local-only Temporal fixture workflow/transport with bounded activity retries/history and OTel propagation; test sender acknowledgement loss, fresh-worker resume, duplicate consumption, and finite dead-letter bounds. No public command route, legacy schedule conversion or production worker registration. Full RG1 remains HELD until all original required increments are evidenced.

P1b local outbox red/green: missing module/runtime produced the intended initial collection failures. Added migration 0018 immutable messages/inbox receipts and finite ordered delivery. Added negative tests for wrong envelope binding and consumed-close acknowledgement loss; both failed before their repairs. Actual worker-kill test initially timed out because child Python imported an older installed package instead of this source tree; the fixture now explicitly supplies the repository `src` path, logs the child and tests the actual implementation. No timeout increase or skipped test. Expanded admission/outbox/runtime/migration focus: **24 PASS**, including subprocess SIGKILL/restart, queued signal delivery, duplicate start reconciliation, captured Temporal history replay and actual SDK/canonical trace correlation. Independent increment review and full regression underway. This local dispatcher/fixture is not enabled on any public API, production worker or automatic schedule.

Independent P1b review found a final-attempt acknowledgement-loss defect: exhaustion could dead-letter a message already consumed and block its successors. Three new regressions reproduced failure for consumed-before-timeout, consumed-after-timeout and consumed-expired-final-lease. Reconciliation now requires a committed inbox receipt and runs before terminal exhaustion; it also recovers late receipts from dead-letter state. Expanded focused suite **27 PASS**, 25.11 seconds. Independent focused re-review confirms the Important issue resolved; timeouts alone never establish successful delivery. Full qualification is rerun after this repair rather than reusing the earlier application run.

Final local outbox checkpoint: **299 full non-live tests PASS**, zero failures/errors/skips, 276.72 seconds; latest focused suite **29 PASS**, 23.84 seconds; **14 documentation tests PASS**, 75-obligation/275-file source and archive/link validation PASS, all six diagrams rendered. Added a real asynchronous transport-timeout regression and a red/green connection-level statement-deadline assertion; every outbox DB operation now has a three-second statement timeout, including acknowledgement/retry/receipt queries. `outbox-increment-evidence.json` preserves exact execution counts and evidence hashes. Earlier 294/298-pass runs precede the last regressions and are not the final qualification. Source is ready for its feature-branch CI; main is not merged and production requirements remain HELD.

Outbox publication verified on `2edad82c3554805713682e2e1a75cd1d631a9555`: both required GitHub checks PASS and strict main protection read back at 2026-09-23T17:14:17Z. Main remains unmerged; independent PR approval is still required. Six diagrams rendered again and raw JUnit hashes/counts matched the durable 29/299-test reports.

### Next bounded increment — goal revision integrity

Using the existing P1 plan and autonomous execution direction: implement revision changes before extending paid allocation or public commands. Current code has immutable revision rows but no revision command; request fingerprints include the current revision, so adding revisions naively would break retries of already committed slots. Design: a scoped `revise_goal` command requires expected revision, bounded idempotency key and reason; locks the goal; appends an immutable revision plus command receipt and audit atomically. Exact retries return their original revision even after later revisions; changed inputs or stale competing commands fail. Old intents keep their identity, unadmitted stale intents deny, existing frozen contexts never change, and recovery/consumption hold on revision mismatch. SQL guards prevent goal identity rewriting, skipped/missing revisions and terminal reactivation. No paid operation, public endpoint, provider selection or automatic scheduler is enabled.

Executable red/green sequence: (1) concurrent CAS and exact/conflicting retry tests, rollback/restart and immutable-context/coalesced-slot tests; (2) additive command-receipt/goal-guard migration and service changes; (3) stale recovery/consumer and direct-SQL guard tests; (4) focused and complete non-live regression, traceability/evidence refresh, independent review and feature-branch CI. Reuse P1a/P1b rather than repeating them. Ruling: stale cycles may close but may not acquire fresh runnable authority; future typed recovery must explicitly bind renewed approval to the original operation. This conservative hold avoids treating a material goal revision as an implicit approval; it can defer otherwise safe local work until the later recovery gate is implemented.

Goal-revision red stage: six tests failed on the missing command. Initial implementation exposed two concurrency failures: a locking join returned no row when another transaction advanced the revision to a payload absent from the original statement snapshot. Locking the goal first and reading its immutable payload in a subsequent statement repaired the actual race; 35 focused tests passed. Independent review then identified an exact-retry conflict after a shortened horizon; a dedicated regression failed before repair. Exact slot lookup now precedes fresh-admission time/state checks but never bypasses current checks when creating successor lineage. Expanded active-goal SQL tests prevent terminal-state guards from masking missing/skipped/backward revision defects. Qualification after these repairs is pending; the earlier 35-pass run is not final evidence.

The review's follow-up identified the same shortened-horizon replay issue for an already committed successor link. A second dedicated regression reproduced it. Exact matching immutable predecessor/successor mappings now replay without authorizing a new link; changed mappings reject and absent mappings still require current authority/window checks. Independent focused re-review confirms both Important findings and the SQL coverage gap resolved. Latest focused admission/revision/outbox/Temporal/migration suite: **37 PASS**, including populated-revision rollback refusal. The earlier full suite was started before these final fixes and cannot qualify the final tree; a fresh complete run is queued after it, with separate `revisions-qualified-regression` artifacts.

Final goal-revision checkpoint: **307 full non-live tests PASS**, zero failures/errors/skips, 277.09 seconds; **37 focused tests PASS**, **14 documentation tests PASS**, six Mermaid diagrams rendered and 277 source/config/test evidence hashes verified. The earlier 305-pass run predates the final retry repairs and is superseded. `goal-revision-evidence.json` preserves exact final commands and JUnit/log/source digests. Dependency consistency, documentation shell contracts and whitespace checks pass. No production capability was enabled. Next executable work remains full goal/authority/baseline/cadence and G0 allocation; full RG1 and external RG0 deployment qualification are still HELD. Publishing this reviewed subincrement on the feature branch for exact-head CI, not merging main.

### Next bounded increment — explicit local baseline approval

Revision source checkpoint `e296d49` is pushed; exact-head CI is running, not yet claimed PASS. Continue P1 locally with explicit goal-revision-bound fixture baseline approval. Reuse `PluginRegistry`/`PluginManifest` for exact pinned contract validation rather than introducing another registry or any dependency. A separate `goals:approve` grant records immutable approval of the fixed no-effect fixture manifest, strategy and prompt versions, expiry and reason for the current goal revision. Admission without that approval requires review and creates no cycle. A material revision never inherits approval. Frozen context stores the approval and planned versions; consumption rechecks expiry/revocation/current revision. Revocation is append-only and serialized by the same goal lock as admission/consumption; it cannot rewrite history or reactivate an old approval. Exact authorized approval retries are idempotent; conflicting payloads reject. No live provider, secret, fallback or general capability lifecycle is introduced.

Red/green plan: add missing/foreign/expired/revoked/revised approval and grant-isolation tests; add additive approval/revocation records with rollback preservation; implement scoped approval commands and registry-based resolver; update fixtures to explicitly approve rather than grandfathering old goals; verify immutable context, concurrent approval/admission/revocation and SQL history preservation; run focused/full regressions and independent review; refresh source/release/traceability evidence and exact-head CI. Existing unapproved local goals hold until explicitly approved; never fabricate approval during migration. Remaining full context/cadence/financial/permit gates remain HELD.

Revision checkpoint CI verified: `e296d49e87dd4aa1fa570bc6347fb8f9b35703f1` passes both required checks and main enforcement at 2026-09-23T17:34:20Z. Baseline red stage produced five failures (unapproved admission incorrectly succeeded; approval commands absent). Initial baseline/migration/outbox/revision focus then passed 42 tests. Expanded actual-expiry, approval rollback, admission/revocation race, unapproved-manifest and populated rollback regressions are being run; independent review underway. No production approval or capability has been fabricated.

Independent baseline review found that recovery could use a new goal-level approval for an old frozen context without any approval reference. A real pre-0020-style persisted-context regression reproduced the bypass. Recovery now requires its original context's exact approval ID, matching consumption; it cannot inherit a later goal approval. Independent focused re-review confirms the Important finding resolved. Final focused baseline/revision/admission/outbox/Temporal/migration suite: **47 PASS**, 33.36 seconds. Full non-live qualification is running on the repaired tree; no partial run is presented as final.

Final local baseline checkpoint: **317 full non-live tests PASS**, zero failures/errors/skips, 284.55 seconds; **47 focused tests PASS**, **14 documentation tests PASS**, six diagrams rendered and 280 reviewed source/config/test hashes verified. Durable `baseline-approval-evidence.json` captures final commands, counts and hashes. Dependency consistency, documentation shell contracts and whitespace checks pass. Independent review repair is verified. Full RG1 remains HELD for context/cadence, G0/G1 accounting, typed case/permit/stop, API/CLI/SDK, automatic dispatch/cutover and upgrade qualification; RG0 production checks remain unchanged. This source checkpoint is for feature-branch CI only, with production effects disabled and main unmerged.

### Exact-head remote qualification checkpoint

At 2026-09-23T17:49:03.182364+00:00, both required GitHub checks (`verify`, `p0-regression`) PASS on `e628ccf478988b4db553983d990f43a3e3ca289a`. Main protection still requires strict app-bound checks and current independent PR approval, including administrators; no force push/deletion or bypass is enabled. The durable repository evidence and release ledger now name this verified application commit. Main remains `8e50d68efbf2bec2aabfb2fe3226e01635de2adb`, unmerged, and the feature branch is pushed. The follow-up documentation-only evidence commit must receive its own fresh remote check; prior-head evidence is never substituted for it. Local source verification is 317 full / 47 focused / 14 documentation tests, six diagrams. RG0 deployment and full RG1 remain HELD with all unresolved obligations explicitly retained.
