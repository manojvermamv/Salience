# V4 blueprint validation evidence

The original documentation-only transition below is historical. The subsequent authorized P0 implementation and fresh verification appear in the final section; earlier no-code-change/browser-missing statements are not the current working-tree state.

Baseline: `8e50d68efbf2bec2aabfb2fe3226e01635de2adb`. Inspection and verification date: 2026-09-22. This record distinguishes documentation validation, current executable behavior, and future production gates.

## Current application observation

Executed `.venv/bin/python -m pytest -p no:cacheprovider tests/contracts tests/unit tests/integration tests/e2e tests/evals -m 'not live' -q`: **226 passed, 6 failed**, one existing Starlette deprecation warning, 243.99 seconds. All six failures are in `tests/integration/test_browser_evidence.py` at browser launch because `/home/admin/.cache/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell` is absent. The suite starts local PostgreSQL/Temporal and applies migrations through its existing fixture setup. No application code changed.

Executed `bash scripts/verify-browser-evidence.sh`: **NOT RUN**, exit 2, because Playwright Chromium is unavailable. Historical 232-pass and browser screenshots are retained as dated evidence, not presented as current results. No browser or application dependency is installed by this documentation task. Live provider calls, V4 behavior and production deployment tests are not run because they are not implemented/configured here.

## Documentation validation sequence

Inventory was persisted first with 360 entries. The complete ArchV4 notes and Mermaid overview were read and incorporated, and code/test/configuration evidence was inspected directly. The blueprint's requirement table specifies 70 obligations. The draft checker passed before any original document was replaced.

**Pre-consolidation gate passed:** `python3 docs/v4/check.py --draft --self-test` returned exit 0 with 70 mapped requirements, both complete V4 sources, 249 locked code/test/configuration files, all 360 original inventory hashes intact, and six negative controls. Manual review checked all twelve V4 sections, the audit-correction table, thirteen contract families, five focused workflows, overview and fifteen release scenarios against the matrix. Two dependency issues found in review were repaired before this pass: intake cannot depend on later capability evolution, and correction/promotion prerequisites cannot form a cycle. Consolidation is authorized by this documented draft pass.

Checkout dependency evidence: `git ls-remote https://github.com/actions/checkout.git refs/tags/v4.2.2` returned `11bd71901bbe5b1630ceea73d27597364c9af683`; the workflow pins that exact commit. This is a documentation-CI dependency only.

No new application tests are written in this documentation-only change. `check.py` is a read-only documentation validator with deliberate negative controls. `.github/workflows/v4-documentation.yml` runs this documentation check on pushes and pull requests without invoking application tests, installing dependencies or deploying. Checkout is pinned to the verified official `actions/checkout` v4.2.2 commit. Remote workflow execution and branch-protection enforcement are not verified locally; P0 must require the check in repository settings.

## Final consolidation verification

- `python3 docs/v4/check.py --self-test`: PASS; 70 requirement mappings, both exact V4 source incorporations, 249 unchanged application/test/configuration files, 360 inventory dispositions, 82 Markdown documents, five local historical HTML artifacts and six deliberate negative controls. Checks cover dependency ordering/cycles, migration/release binding, source/evidence drift, preservation hashes, active local links and historical heading anchors.
- Preservation audit: all 62 archived originals reconstruct to their inventory SHA-256; five base64 snapshots preserve a missing final newline exactly. All remaining original inventory entries are retained. Worktree/local-only artifacts are checked when present but are not required in a new checkout. No original instruction file is moved or changed.
- Simulated clean checkout: copied only tracked and newly added non-ignored files into a temporary directory and invoked the same validator. PASS with 82 Markdown files and four tracked historical HTML artifacts. The fifth local HTML artifact is ignored visual evidence, not a CI prerequisite. This tests checkout portability without claiming remote CI execution.
- Existing documentation checks: `bash tests/scripts/test_phase_7_8_documentation.sh`, `bash tests/scripts/test_phase_9_documentation.sh`, `bash tests/scripts/test_browser_documentation.sh`, and `bash tests/scripts/test_verify_browser_evidence.sh` pass. No application test or verifier was rewritten to accommodate the documentation transition.
- `git diff --check` passes. Application source, tests, migrations, runtime scripts, dependency pins and deployment configuration retain baseline hashes. HEAD remains `8e50d68efbf2bec2aabfb2fe3226e01635de2adb`; no commit/history rewriting or deployment occurred during this task.

Scope of link validation: all active repository Markdown navigation, local targets in retained HTML, and preserved original heading bookmarks. Historical snapshots intentionally retain original text as evidence, not executable Markdown links. Public-reference availability and remote repository settings are outside this local check and are reverified at the relevant production release gate.

The blueprint is the standalone future execution contract: target source text/diagrams, retained product requirements, current code evidence, P0–P7 implementation sequence, 70 requirement mappings, migration/rollback, operating runbooks and 20 conflict/unknown dispositions are present without requiring an archived plan. Future application work and business/account-specific production decisions are explicitly open; this documentation validation does not mark them implemented.

## Authorized blueprint correction and P0 implementation — 2026-09-23

**Scope:** corrected blueprint first, then only P0 application changes. Original baseline and archive hashes are retained; no Git history rewrite, commit, push, production deployment or production effect occurred. P1–P7 code was not started.

### Corrected planning and documentation evidence

- Removed forward obligations from RG2/RG3/RG4 and bound each prose gate to all rows owned by its phase. The 70 original requirements remain, with five foundational splits for early authority/fallback, baseline decisions, trial isolation and registry namespace separation. Full outbox tracing belongs to RG1. Requirement DAG, phase/gate ordering and expanded range-reference tests pass.
- Blueprint section 6 resolves re-admission revision/idempotency, G0/G1 allocation transfer and unresolved liabilities, mandatory/optional capabilities including R58, and promotion/invalidation serialization before their implementation phases.
- `python3 docs/v4/test_checks.py`: 7 tests pass, including regressions for forward/range gate dependencies, malformed Markdown and falsely reporting a NOT RUN gate as released.
- `python3 docs/v4/check.py --self-test`: passes 75 obligations, both exact source incorporations, 262 refreshed code/config/test evidence files, 360 dispositions, 82 active Markdown documents, five local HTML files and six original negative controls. A simulated clean checkout also passes (four tracked HTML files; local ignored visual evidence is optional).
- Markdown heading/fence/table/link checks and literal command/path inspection found no actual export corruption, so no undamaged original prose was rewritten as a supposed repair. Original sources still match their original SHA-256 values. All six Mermaid diagrams parse and render using Mermaid 12.0.0 with system Chromium; six SVG/PNG outputs and input/output hashes are in the regenerable local diagram artifacts.
- Four existing documentation/command-contract shell checks pass. `git diff --check`, Compose P0 configuration validation with non-secret placeholder values and `.venv/bin/python -m pip check` pass. Remote CI execution/branch protection is NOT RUN.

### Implementation and repair evidence

- The first documentation tests failed for a nonexistent gate checker and P0's premature outbox obligation, then passed after correction. New identity/storage/retention/secret tests initially failed on missing implementation; trace tests reproduced zero/uppercase identifier acceptance. No red failure was treated as a completion state.
- Identity uses fixed-algorithm PyJWT verification and current PostgreSQL subjects/grants, not caller-provided scope headers. The P0 profile only allows scoped local control/read operations; provider/agent/workflow/schedule/callback paths and workers cannot dispatch. Tests cover expired/revoked/foreign authority, wrong audience/key, header injection, disabled-operation grants, request bounds and append-only actor audit.
- Storage tests use both fakes and actual disposable services. Garage 2.3.0 demonstrably ignores conditional writes; its negative regression verifies rejection before storing caller bytes. SeaweedFS 4.47 passes exact retry, immutable conflict, concurrent create, corruption/missing readback, bucket-scoped credential and PostgreSQL retention/quarantine tests. This replaces the initial two skipped positive storage tests, not their requirements.
- An initial broad regression returned 249 pass/1 failure/2 skip/7 deselected: memory integrity metadata had changed a browser fixture contract. Repaired by retaining a private hash while preserving caller metadata. A subsequent browser-enabled run returned 259 pass/2 skip before the qualified storage replacement. Another finalization run returned 266 pass/1 failure from a real storage data-plane startup timeout; bucket metadata readiness was insufficient. The fixture now warms and verifies put/get/delete before handing over bounded, bucket-scoped credentials. Those intermediate results are retained, not reported as final green.
- Local inspection repaired the delete-before-database-commit race: reference acquisition now reads/verifies bytes under the row lock, persists quarantine and rejects a dangling reference. It also repaired API parent-span continuity, disabled raw exception-message export, enforced inactive-workspace denial and bounded object reads/writes. Credential policies pin workspace/destination/audience/provider/version/exact scope, recheck expiry at reveal, support revocation and prevent legacy-resolution bypass/serialization.
- Focused final P0 command `.venv/bin/python -m pytest -q tests/integration/test_v4_*.py tests/unit/test_v4_deployment_boundary.py tests/unit/test_v4_secret_boundary.py`: **37 passed**, one pre-existing Starlette/AnyIO deprecation warning. This includes fresh-database upgrade, empty rollback/re-upgrade and populated-table rollback refusal. The production-safe rollback is configuration suspension, not deletion of audit/identity/tombstones.
- Pinned Playwright headless Chromium 151.0.7922.34 (revision 1234) and its FFmpeg runtime were installed as local test tooling. Existing browser tests now run; browser capability remains disabled in P0.
- Local inventory capture records 87 tables at migration 0014, with local fixture history/external-ID/reservation references and installed dependency/route manifests. No production history inventory was possible. A separate pre-migration database snapshot was not captured for this disposable local experiment; the original source/schema baseline is retained, and production cutover requires its own pre-change backup and inventory.

### Release disposition

Final full non-live regression: **269 passed, zero failures, zero skips**, one pre-existing Starlette/AnyIO deprecation warning, in 254.27 seconds. Command: `.venv/bin/python -m pytest -p no:cacheprovider tests/contracts tests/unit tests/integration tests/e2e tests/evals -m 'not live' -q -rs --junitxml=artifacts/v4-p0/final-regression.xml`. This final run includes the storage readiness repair, scoped-secret tests, existing browser/e2e/durable-recovery suites and all P0 tests on the final application tree. JUnit and full/focused/red-stage logs are preserved under `artifacts/v4-p0/`; the compact release ledger persists the results in the repository. Live-provider checks remain intentionally NOT RUN.

**RG0 production release is HELD.** [The release ledger](p0-release.json) separates local implementation qualification from NOT RUN external requirements. All five production profile environment inputs are absent: `P0_DATABASE_URL`, `P0_WORKSPACE_ID`, `P0_IDENTITY_ISSUER`, `P0_IDENTITY_AUDIENCE`, `P0_IDENTITY_PUBLIC_KEY_FILE`. Approved TLS/issuer/rotation, restricted production database role, encrypted managed storage/backup restore, remote collector/alerts and repository enforcement have not been supplied or certified. No values or approvals were fabricated.

The independent review agent was requested per the review skill, but the service returned an account usage-limit error before providing findings. Independent review is therefore **NOT RUN**, not passed; local inspection cannot substitute for production review. These external qualifications must be supplied and verified before RG0 is released or production effects are considered. The P0 profile remains effects-disabled and no later phase was started.

### Subsequent review correction and development entry

The paragraphs above are historical P0 qualification evidence. The subsequent user request explicitly separates DG1 local development from RG0 deployment. On the corrected tree, 39 focused P0 tests and 271 full non-live tests passed, with zero failures/skips. Twelve documentation regressions and all six Mermaid renders passed. The durable [development-entry evidence](development-entry-evidence.json) records commands, execution counts, source-lock digest and local raw-evidence hashes. This qualified DG1 before any P1 implementation; it did not release RG0.

- Release validation now requires the exact mandatory external-check multiset, recognized statuses, successful evidenced local qualification, and nonempty evidence for each PASS. Negative cases cover missing/empty/duplicate/unknown checks, failed/skipped/error/unsuccessful execution, absent evidence and non-repository evidence masquerading as durable. Durable reports validate actual command result fields and JUnit digests. The raw local files remain optional in a clean checkout; the committed report is mandatory and hash-checked.
- A genuine non-owner, non-superuser, non-BYPASSRLS database login authorizes successfully with SELECT, audit INSERT and narrow function EXECUTE, but cannot modify authority or its schema. Revocation ordering is tested with actual PostgreSQL row locks and a competing writer, not mocked privilege checks. Migration 0015 removes the need to grant the application identity UPDATE rights without removing the lock.
- Read-only GitHub inspection found baseline documentation run [35811429145](https://github.com/manojvermamv/Salience/actions/runs/35811429145) successful, but main unprotected and no applicable rulesets. Main was then protected with strict app-bound `verify` and `p0-regression`, administrator enforcement, required current independent PR review, no force pushes/deletion and resolved-conversation enforcement. Readback passes; until the new regression workflow runs successfully on the implementation head, CI/merge remains HELD. `verify_repository.py` produces fresh commit-bound evidence and rejects missing, pending, skipped, failed or stale checks. Branch protection, not an imaginary ruleset, supplies enforcement.
- P1a is the private dry-run-only admission/lifecycle slice described in the blueprint. The initial import failure and the subsequent direct-SQL identity-rewrite failure were observed before implementation/repair. No workflow start, paid reservation, provider invocation, API surface or live scheduler was added. The full P1 outbox, budget-transfer, permit/review, Temporal replay and API/CLI/SDK gates remain NOT RUN and RG1 remains HELD.
- A fresh independent code reviewer found two additional release-validator bypasses (ignored test errors and host-local files accepted as repository evidence). Both received negative regressions and fixes. This is engineering code review, not approval of the still-unconfigured production identity, storage/restore, telemetry or deployment.

Reproduce local gates with `python3 docs/v4/test_checks.py`, `python3 docs/v4/check.py --self-test`, and `.venv/bin/python -m pytest -p no:cacheprovider tests/integration/test_v4_cycle_admission.py tests/integration/test_v4_identity_isolation.py tests/integration/test_v4_migrations.py -q`. Before any implementation merge, additionally run `python3 docs/v4/verify_repository.py --commit HEAD --output artifacts/v4-p0/repository-current.json` with read-authorized GitHub credentials. A successful local command is not remote CI evidence; independent PR approval is still required after checks pass.

### Incremental local outbox verification

The initial remote application run failed with 280 passing tests and four storage fixture setup errors; the pinned image dropped to UID 1000 while the GitHub runner's mode-0600 credentials had another owner. Matching the container/data-mount UID/GID to the fixture owner fixed the real permission mismatch without weakening file permissions or removing tests. Both required checks then passed on `b1969c8`; [repository enforcement evidence](repository-enforcement-evidence.json) records their exact head and read-back main protection. This is not an approval for a different future head.

P1b adds only the local outbox/consumer/Temporal fixture described in the blueprint. The focused outbox/admission/migration/runtime suite passes 27 tests. Real-process SIGKILL, lost acknowledgement, duplicate start, queued signals during downtime, a new worker, and recorded-history replay all execute against real PostgreSQL and Temporal. OpenTelemetry SDK span IDs match canonical consumption events. No live provider is called. An independent reviewer identified a final-attempt acknowledgement-loss bug; three red regressions proved it, and re-review accepted the receipt-before-exhaustion repair. Failed delivery alone does not count as consumption. Full production typed cases, automatic dispatcher service, API parity and G0/G1 financial allocation remain separate uncompleted gates.

Final qualification adds transport-timeout and universal DB-statement-deadline tests: **29 focused PASS**, **299 full non-live PASS** with zero failures/errors/skips, **14 documentation tests PASS**, six diagrams rendered, and 275 reviewed source/config/test hashes verified. The only application-suite warning is the existing Starlette/AnyIO deprecation. [Outbox increment evidence](outbox-increment-evidence.json) records the commands, current source-lock hash, raw JUnit/log hashes and independent review outcome. Historical 294/298-pass runs are superseded by this final 299-pass run, not silently relabeled.
