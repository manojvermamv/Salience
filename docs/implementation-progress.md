# Implementation Progress

## Active Context

- Branch: `intelligence-loop-phases-5-6` (isolated worktree)
- Plan: `docs/superpowers/plans/2026-09-12-intelligence-loop-phases-5-6.md`
- Active task: Pre-Phase-5 compatibility/trust gate plus Phases 5–6 intelligence loop.
- Current TDD step: All planned Pre-Phase-5 and Phase 5–6 tasks are complete; begin Phase 7 only from a selected immutable `ContentBrief@v1`.
- Last verified state: Task 12 completed. The post-wiring focused suite passed 16/16: configuration, configured RSS contract, restart-safe Phase 5 workflow, full Phase 5–6 ContentBrief lineage, ranking/packaging/safety evals, and intelligence control API. `compileall` and `git diff --check` passed. Earlier, `pytest tests -m 'not live' --ignore=tests/e2e/test_compose_worker_restart.py -q` completed against active PostgreSQL/Temporal with no entries in pytest's `lastfailed` cache. The active database is upgraded through `0005_strategy_idempotency`. The local host is at 99% usage with 319 MB available; `docker system df` reports 6.669 GB images (948.8 MB reclaimable), 903.1 MB volumes (0 reclaimable), and 304.2 MB build cache (0 reclaimable). No browser binary, broad isolated Compose run, or destructive cleanup was attempted. `scripts/verify-phases-1-4.sh` remains previously verified, but was not re-run under this capacity constraint.

## Checkpoints

- 2026-09-12: Created the isolated worktree and installed the local test runner.
- 2026-09-12: Added and verified the failing configuration-security test; implementation has not started.
- 2026-09-12: Completed dependency research and recorded ADRs; SeaweedFS was rejected for an unpatched high advisory and Garage selected as the isolated S3 service.
- 2026-09-12: Implemented the validated settings model, pinned initial dependencies/images, passed the configuration test and Compose validation, and built the Python 3.13 image.
- 2026-09-12: Completed and committed the deployable runtime scaffold, then observed the focused PostgreSQL migration test fail before migration implementation.
- 2026-09-12: Implemented immutable canonical PostgreSQL migration and matching typed models; verified required identity, governance, provenance, scheduling, idempotency, trace, and secret-reference schema invariants against live PostgreSQL.
- 2026-09-12: Completed and committed the canonical database Task 2 checkpoint, then created the four focused red governance contracts for policy, costs, secrets, and tracing.
- 2026-09-12: Implemented deterministic governance controls and repaired an installed OpenTelemetry trace-flag compatibility mismatch; all six focused governance tests pass.
- 2026-09-12: Completed and committed the Task 3 governance checkpoint, then created red conformance tests for memory/S3 object stores and versioned plugin registration.
- 2026-09-12: Implemented and verified owned storage, workflow, and plugin contracts with memory and mocked-S3 adapter coverage; Garage v2.3 command/configuration surface was inspected before wiring a real runtime adapter.
- 2026-09-12: Completed and committed Task 4; started the pinned Temporal runtime and observed the red process-restart verifier fail before any workflow implementation.
- 2026-09-12: Implemented a real Temporal worker restart scenario with independent idempotent mock-effect reconciliation; focused e2e verifier passes against the pinned Compose Temporal service.
- 2026-09-12: Expanded the restart verifier with canonical checkpoint/effect/audit/provenance assertions and observed the expected red database-boundary failure.
- 2026-09-12: Root-caused a native asyncpg/Python 3.13 crash to PostgreSQL I/O inside Temporal activities. Plain repository I/O is stable, but three activity-boundary variants (original connection, explicit non-TLS connection, and pre-opened connection) segfaulted. The unsafe uncommitted prototype was removed. Psycopg 3.3.5 is the selected next experiment: its official metadata supports Python 3.13, but its LGPL-3.0-only license must remain recorded in the dependency decision before adoption.
- 2026-09-12: Adopted Psycopg 3.3.5 for the worker-side repository after its checked live Temporal restart scenario persisted canonical checkpoints, reconciled effect state, audit events, and provenance without the asyncpg crash.
- 2026-09-12: Added and verified durable terminal-state handling: three-attempt retry exhaustion to a canonical dead letter, activity timeout, cooperative cancellation, approval and budget denial before effects, and dry-run execution with no provider call.
- 2026-09-12: Added a deployable worker, independent HTTP mock provider, Temporal schedule adapter, and isolated Compose recovery verifier. The verifier builds a unique stack, migrates it, hard-exits the worker after provider acceptance, restarts it, verifies reconciliation, and removes only that stack.
- 2026-09-12: Completed Phase 1 control-plane coverage. The FastAPI adapter enforces token and scope checks, creates canonical workspace/program identities, starts idempotent dry-run jobs through Temporal, exposes inspection records, and supplies an HTTP-only CLI. The live Phase 1 verifier passes.
- 2026-09-12: Added Phase 2 canonical callable-agent entities: immutable provider-neutral manifests, version registry with disabled-history preservation, and PostgreSQL agent/team/run/delegation/event records.
- 2026-09-12: Added deterministic native agent execution for the lead, research, and strategy fixture manifests. Direct/delegated equivalence, schema rejection, and team composition are verified without an AI/model provider.
- 2026-09-12: Completed Phase 2 public callable-agent surfaces. The control API, SDK, and CLI share the same agent schemas; the Phase 2 verifier confirms direct, delegated, and team fixture calls remain equivalent.
- 2026-09-12: Added Phase 3 model gateway contracts with deterministic static fixtures and an optional schema-validating OpenAI-compatible HTTP adapter. Agent runtime selection remains outside agent manifests.
- 2026-09-12: Researched the current official MCP specification and SDK. Implemented an SDK-free `2025-11-25` fixture gateway so the project owns its compatibility boundary while the breaking v2 SDK remains deferred.
- 2026-09-12: Researched official A2A v0.3.0 and its Apache-2.0 Python SDK. Added an SDK-free version-gated descriptor/task/artifact fixture and completed the Phase 3 verifier suite.
- 2026-09-12: Added scoped PostgreSQL memory plus versioned research-evidence and strategy persistence. Cross-program scope isolation is verified and no vector database is used.
- 2026-09-12: Completed fixture-backed niche bootstrap. A niche creates canonical workspace/program identities, direct research/strategy agent runs, evidence, scoped memory, and immutable explainable strategy records without browser or model-provider access.
- 2026-09-12: Completed the clean Phases 1–4 verification script and operational runbooks. Its isolated Compose run applied every migration and passed Phase 1 recovery/control, Phase 2 agents, Phase 3 protocols, and Phase 4 bootstrap checks.
- 2026-09-12: Merged the verified `phases-1-4-foundation` branch into local `main`, preserving the user-owned README product line for the requested master README refresh.
- 2026-09-12: Installed the project-local Archify 2.17 skill, verified its Node 24.19.0 runtime with `doctor`, authored a repository-grounded Phase 1–4 architecture specification, and delivered a standalone interactive HTML artifact. The final Archify showcase receipt reports 9/9 checks, zero composition errors, and zero warnings. Browser evidence is skipped because Chrome/Chromium is unavailable in this environment; the deterministic delivery artifact remains valid.
- 2026-09-12: Replaced the minimal README with an end-user master guide covering the shipped scope, safe in-container demo, public API, validation, architecture, extension points, and deliberate exclusions. The README links the checked Archify viewer and retains the product line.
- 2026-09-12: Re-ran `git diff --check`, verified all README repository references, and re-checked the frozen Archify HTML. The artifact remains a 9/9 showcase pass with zero errors and warnings; no Docker workload was started because the host still has insufficient free space for a safe Temporal test rerun.
- 2026-09-12: The `main` checkout intentionally has no duplicate virtual environment. Reused the retained Python 3.13 test environment from the verified worktree against the already-running local PostgreSQL/Temporal services; `tests/integration/test_migrations.py` passed 2/2 in 1.65 seconds. This adds fresh migration evidence without creating a new Temporal volume.
- 2026-09-12: Started the isolated `intelligence-loop-phases-5-6` worktree from current `main` after reading the new end-to-end direction, all core-pack Markdown, current contracts, migrations, repositories, test tree, disk state, and existing verified infrastructure.
- 2026-09-12: Independently researched and selected official MCP Python SDK 2.2.0 for MCP `2026-07-28`, A2A SDK 1.1.2 for A2A 1.0 with explicit 0.3 compatibility, and optional Playwright 1.62.0 for governed browser research. The host has insufficient space for browser binaries; only optional, lazy browser setup will be added until an operator authorizes storage recovery.
- 2026-09-12: Persisted the approved autonomous-build design at `docs/superpowers/specs/2026-09-12-intelligence-loop-phases-5-6-design.md` and the placeholder-free twelve-task TDD plan at `docs/superpowers/plans/2026-09-12-intelligence-loop-phases-5-6.md`. Review confirmed every explicit compatibility, trust, model, source/browser, Phase 5, Phase 6, durability, evaluation, documentation, and Phase-7 handoff requirement maps to tasks.
- 2026-09-12: Completed Task 1. The intentional red configuration contract failed on five missing optional settings/limit validations. Added validated, secret-reference-only intelligence settings; pinned MCP 2.2.0 and A2A SDK 1.1.2; made Playwright 1.62.0 an optional extra; and recorded the build-vs-adopt ADR plus exact protocol compatibility matrix. `tests/test_config.py` then passed 6/6 and `git diff --check` was clean.
- 2026-09-12: Completed Task 2. The red protocol contracts failed at missing adapter imports. Added thin official SDK adapters that project MCP 2.2.0 discovery/read-only tool calls and A2A SDK 1.1.2 Agent Card/task artifacts into owned DTOs; protocol objects do not cross the boundary. Added protocol feature/auth/SDK metadata, explicit legacy gates, semantic A2A version handling, and remote task lineage. One missing SDK-provenance projection was root-caused and repaired at the owned gateway. The full focused protocol suite then passed 8/8 and `git diff --check` was clean.
- 2026-09-12: Completed Task 3. The red trust tests failed at the missing trust module. Added a minimum-authority `TrustContext` and fail-closed `TrustPolicy`; untrusted sources are read-only, receive no tool/delegated authority, and can write only sanitized unverified evidence memory. The existing canonical memory columns now persist source, verification, writer, provenance, and trace metadata through an explicit context. Agent execution contexts intersect child manifest scopes with parent authority. A 16-column insert initially had 17 placeholders; the database failure identified and the focused parameter mismatch was repaired. An adjacent runtime contract then exposed one missed root-invocation context argument, which was repaired. The focused trust/delegation/runtime suite passed 6/6 and `git diff --check` was clean.
- 2026-09-12: Completed Task 4. The red model contracts failed at the missing recording decorator. Added schema-validating, hash-only model invocation lineage that records successes, invalid outputs, and failures without persisting prompts or credentials. `ModelRequest` now carries capability, parent-agent/job, trace, and artifact references; `ModelResult` exposes provider/model/version, hashes, output artifact reference, and actual-cost hook. The OpenAI-compatible edge accepts an injected HTTP client for deterministic testing and records real usage fields; static fixtures preserve current behavior. A one-character secrecy assertion produced a false positive through unrelated text/hash content, so it was corrected to a unique sentinel. The model/runtime suite passed 5/5 and `git diff --check` was clean.
- 2026-09-12: Completed Task 5. The red connector/browser contracts failed at absent owned DTOs and adapters. Added bounded direct HTTP with HTTPS/domain allowlists, disabled redirect following, content-type/byte checks, timeout, and retry-after provenance; RSS/Atom preserves GUID/link/publication time and Hacker News maps only documented item fields/available score/comments. Added a lazy, read-only Playwright adapter with per-run context, no downloads, route allowlisting, artifact-only text/screenshot/trace results, and a clear unavailable error. Browser package/binary installation was not attempted because storage remains constrained. The deterministic suite passed 3/3, and `tests/live/test_research_smoke.py` is explicitly skipped without `LIVE_HACKER_NEWS_ITEM_URL`.
- 2026-09-12: Completed Task 6. The initial migration contract failed because the intelligence package/schema was absent; its helper import was also corrected because the repository's test tree is intentionally non-package. Added migration `0004_intelligence_loop` with canonical sources, idempotent fetches, evidence extensions, signals/support, opportunities, queue, packages/evaluations, claims/evidence, immutable briefs, and model invocation lineage. Added typed ORM metadata, a hash-only PostgreSQL model-invocation repository, and an idempotent Psycopg `IntelligenceRepository`; a fetch retry returns its original identifier and the brief lineage traverses source/fetch/signal/opportunity/package/claim rows. Trust context metadata is now stored on memory rows. The migration/repository/memory suite passed 6/6 and `git diff --check` was clean.
- 2026-09-12: Completed Task 7. The red signal tests failed at the missing normalizer/ranker modules. Added deterministic URL/topic normalization, raw feature preservation, explicit present/missing availability, stable SHA-256 fingerprints, and source-support-preserving merge groups. Source diversity is derived only from retained source identities. The transparent weighted score is clamped to `[0, 100]`; a model adjustment is independently clamped to `[-10, 10]` and cannot replace source facts. The normalization/ranking/eval suite passed 4/4 and `git diff --check` was clean.
- 2026-09-12: Completed Task 8. The direct/delegated contracts failed because the browser agent manifest and versioned result payloads were absent. Added injected Research, Strategy, and read-only Browser runtimes emitting `ResearchResult@v1`, `StrategyProposal@v1`, and `BrowserResearchResult@v1`; they retain provider-neutral source-linked findings, contradictions, questions, strategy assumptions/uncertainties, and artifact-only browser results. The deterministic fixture service supplies no browser/model provider. Two legacy fixture tests identified changed source provenance, so the injected fixture runtime explicitly retains the legacy `fixture` label rather than changing existing contract meaning. The intelligence/legacy-agent suite passed 5/5 and `git diff --check` was clean; model-invalid/unavailable outcomes remain covered by Task 4's recorded gateway failure contracts before an agent run is built.
- 2026-09-12: Completed Task 9. The intentional red durable contract failed on the absent `salience.workflows.intelligence` module. Added one registered Temporal `IntelligenceLoopWorkflow` with source-fetch, normalization, ranking, delegated Research/Strategy, strategy proposal, queue, completion, cancellation, bounded retry/backoff, and canonical checkpoints. Each stage has a normal job/trace/audit record; fetch/evidence/signal/strategy/queue writes are idempotent, and agent/delegation lineage is persisted under deterministic stage run IDs. The test interrupted a worker after `research.fetch.persisted`, resumed it on a replacement worker, and proved a single canonical fetch. Added retry-safe strategy proposal identity migration `0005_strategy_idempotency`, authenticated start/inspect/schedule routes, and matching CLI/SDK surfaces. The deployable worker now registers both existing and intelligence workflows on one owned task queue. `tests/integration/test_intelligence_control_api.py`, `tests/e2e/test_phase5_durable_research.py`, `tests/e2e/test_phase1_verifier.py`, `tests/unit/test_cli.py`, `tests/unit/test_sdk.py`, and `tests/unit/test_schedules.py` passed 7/7; compile and `git diff --check` passed.
- 2026-09-12: Completed Task 10. The intentional red contracts failed because strategic packaging, claim verification, and brief assembly modules did not exist. Added a deterministic 3–5 candidate generator with required audience/problem/angle/hook/promise/format/duration/opening/novelty/evidence/risk fields and normalized diversity fingerprints. The evaluator applies visible required-field, evidence, positioning, and misleading-hook penalties; optional semantic scores are bounded and cannot replace deterministic scoring. Selection records the winning score and reason. Claim verification only promotes explicitly verified supporting evidence and marks contradictions as `contradicted`. The immutable `ContentBrief@v1` assembler carries only verified claim IDs; unsupported external claims are explicitly prohibited, with risks/questions and a `phase_7_review` handoff. Package/claim/brief integration preserved canonical lineage. The focused suite passed 7/7 and `git diff --check` was clean.
- 2026-09-12: Completed Task 11. The red full-loop verifier reached a completed Phase 5 run but correctly failed because it had no `content_brief_id`. Added package generation/evaluation/selection, claim/evidence link, and brief activities to the same Temporal workflow; they use canonical IDs, bounded workflow activity retries, checkpoints, and immutable/idempotent PostgreSQL rows. The final verifier proved lineage from a selected `ContentBrief` back to source/fetch/signal/opportunity/package/Lead Agent. Added the versioned selected-opportunity brief route (`ContentBriefRequest@v1`), brief and lineage inspection, and matching CLI/SDK commands. Prompt-injection and conflicting-evidence evals prove that hostile text cannot become trusted memory and contradictory claims cannot be verified. The Phase 5–6 slice passed 9/9; compile and `git diff --check` passed.
- 2026-09-12: Completed Task 12. Updated the master README, architecture/deployment/verification/limitations docs, and delivered research, trust, model-execution, and Phase-7-handoff runbooks. Documentation names the current MCP/A2A SDK boundary, API/feed-first research, optional browser installation, source scope, model capture, trusted-memory restrictions, signal/strategy lifecycle, package/claim/brief lifecycle, and deferred Phase 7 capabilities without claiming browser binaries, credentials, publishing, media, analytics, or learning. Added opt-in `RESEARCH_RSS_FEED_URLS` worker configuration; it reuses the native Research contract, requires exact HTTPS allowlisting, and remains untrusted. The extended config/connector/intelligence-agent suite passed 11/11. A full non-live suite against active services had no recorded failures; `compileall` and `git diff --check` passed. Storage measurement prevented a fresh browser download and isolated Compose rerun; no destructive cleanup was performed.
- 2026-09-12: Re-verified after final RSS-worker wiring. The focused configuration/connector/durable-workflow/full-lineage/eval/control suite passed 16/16 in 15.79 seconds with one third-party Starlette deprecation warning. `compileall`, `git diff --check`, and diff-stat inspection passed.
- 2026-09-12: Rebuilt the master user-facing documentation for the implemented Phase 1–6 boundary. Replaced the superseded Phase 1–4 Archify source/viewer with repository-grounded `docs/salience-phase-1-6.architecture.{json,html}` and aligned README, API, architecture, workflow, recovery, data, governance, agent, adapter, protocol, dependency, research, trust, deployment, verification, limits, memory, plugin, and local-development guides. Archify delivery froze specification SHA-256 `bd170f68d428df610b8c34ccfe98f871e671617698645f66ec60c3638046338b` and artifact SHA-256 `dd7fc33a192e7cb3b2f259f0f5d7997fc3903c76c60ffaf174158a4c6c84de48`; its showcase receipt passed 9/9 checks with zero errors and warnings and 15 revision-pinned source references. Automated browser evidence is truthfully skipped because Chrome/Chromium is unavailable; no perceptual visual review was performed.
- 2026-09-12: Revalidated the frozen Phase 1–6 Archify source and HTML after the documentation refresh: each passed all 9 showcase checks with zero errors/warnings. `git diff --check` passed and all 25 local Markdown links resolved. Browser evidence remains skipped only because this host has no Chrome/Chromium executable.

## Resume Instructions

0. 2026-09-12: Began the governed browser-evidence prerequisite. Created isolated worktree `browser-evidence`, read the browser-evidence prompt and current browser/agent/storage/trust contracts, and recorded the install/build-vs-adopt decision in `docs/superpowers/specs/2026-09-12-browser-evidence-design.md`. No browser, Docker, or system state has changed yet.
0. 2026-09-12: Completed browser-evidence Task 1. `BrowserResearchRequest` now rejects non-positive bounds and carries owned agent/tool/trace IDs. Shared HTTPS scope validation denies private IP literals unless an explicit test-only flag permits the local fixture. Focused unit tests passed 7/7; compilation and diff checks passed.
0. 2026-09-12: Completed browser-evidence Task 2. The existing Playwright adapter is now explicitly headless, has one context per run with downloads disabled, limits routed requests, retains text/PNG/ZIP artifacts, and writes artifact hashes plus source/fetch/version/run metadata. Response-limit failures keep a trace reference without leaking content. Focused browser-policy tests passed 9/9; compilation and diff checks passed.
0. 2026-09-12: Completed browser-evidence Task 3. Agent execution allocates the canonical run UUID before invoking a runtime and persists the same UUID on `AgentRun`. The browser agent forwards agent/tool/trace IDs through the existing owned request DTO while retaining its artifact-only `untrusted_external` result. Focused agent and trust tests passed 8/8; compilation and diff checks passed.
0. 2026-09-12: Completed browser-evidence Task 4. Added the project-owned idempotent setup and single-command verifier scripts. The setup uses only the official Playwright Chromium dependency/browser commands, measures disk, and performs no cleanup; the verifier distinguishes `NOT RUN` from real test results and retains `artifacts/browser-evidence/` evidence. Shell syntax and missing-prerequisite smoke checks passed.
0. 2026-09-12: Completed browser-evidence Task 5 on Debian 13. `bash scripts/setup-browser-evidence.sh` installed Playwright 1.62.0 Chromium 151.0.7922.34 with the documented `install-deps chromium` then `install chromium` sequence; free disk changed from 5.9 GiB to 5.3 GiB. The project-owned `bash scripts/verify-browser-evidence.sh` passed 7/7 real headless Chromium tests for JavaScript evidence, artifact metadata/hashes, allowlist/redirect/private scope, downloads, timeout traces, and hostile-text trust isolation. Retained proof is under `artifacts/browser-evidence/20260912T183609Z-624394/` (ignored by Git).
0. 2026-09-12: Completed browser-evidence Task 6. Updated the README and operator/research/verification/limits guides with the one-command optional browser workflow, evidence retention, private-network exception boundary, and untrusted-text policy. Final project verifier evidence is under `artifacts/browser-evidence/20260912T184149Z-629620/` (ignored by Git) and passed 7/7; focused browser/agent/trust regressions passed 17/17. The refreshed `docs/salience-phase-1-6.architecture.html` has specification SHA-256 `84a629374d17c38df59516b1209cb688aa6c50fbba83e0f126774283b2e4436c` and artifact SHA-256 `6be50600e7b918f5c99ef6bd6eefe1ad444d0f02da7c85556e9f17281f099760`; Archify validation passed 9/9 with zero errors/warnings and automated Chromium evidence passes containment/readability at 1440x900, 1600x1000, 1920x1080, and 2048x1320. An image-capable spot review found the light 1440x900 composition readable and balanced.
0. 2026-09-12: Revalidated the merged `main` checkout. The full suite passed 99/99 executed tests with one intentional skip and one third-party Starlette deprecation warning; the default `bash scripts/verify-browser-evidence.sh` then passed 7/7 using `/home/admin/Salience/.venv` without an override. Main-checkout setup retained Playwright 1.62.0 and Chromium 151.0.7922.34, measured 4.1 GiB free before its isolated venv setup and 3.7 GiB after, and wrote final local evidence to `artifacts/browser-evidence/20260912T185226Z-647576/`.

1. Start Phase 7 from `docs/phase-7-handoff.md` and a selected `ContentBrief@v1`; do not reconstruct or mutate Phase 5–6 research decisions.
2. Reuse the retained test environment/active services for focused verification and checkpoint every verified task.
3. Before any browser binary or isolated Compose stack, inspect storage and request authorization before removing any persistent project volume.
